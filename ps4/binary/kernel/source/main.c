/*
This may one day be filled.
The idea is to have an elfloader,
which can be loaded to kernel and act as an in-kernel loader.
Not too complex, but not the highest priority either.
*/

int main(int argc, char **argv)
{
	return 0;
}
